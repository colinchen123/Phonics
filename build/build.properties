# IMPORTANT Change to your local system paths before using ANT

#Toggle on/off specific targets
run.test = true
run.asdoc = true
autolaunch.url = true

# Build paths
FLEX_HOME = ${env.FLEX_HOME}

#Properties file for build.xml
project.name=NaliGame

#Version number
ver.num=v2.0
project.name.versioned=${project.name}-${ver.num}

target.player=10.2.0
target.version=11
#browser
browser = "C:/Users/SeanZou/AppData/Local/Google/Chrome/Application/chrome.exe"
report.url = ${report.loc}/html/index.html
msgs=hello,1,2,34,5

dir.bin=${dir.root}/bin-debug
dir.tmp=${basedir}/tmp
dir.ext=${dir.root}/extension
dir.p12=${basedir}/certificate/avatar_develop.p12
dir.provision=${basedir}/certificate/MomoDevelopment.mobileprovision
# ------------------------------------------------------------------------------
# Compiling modules setting
# ------------------------------------------------------------------------------
adt.jar=${FLEX_HOME}/lib/adt.jar
#build locations
bin.loc=${basedir}/release
#${basedir}/bin
asdoc.loc=${FLEX_HOME}/bin/asdoc
main.src.loc=${basedir}/src
test.src.loc=${basedir}/test
doc.loc=${bin.loc}/doc
prolib.loc=${basedir}/libs
exlib.loc=${basedir}/exlibs
lib.loc=${basedir}/build/libs
report.loc=${bin.loc}/report
template.loc=${basedir}/build/templates
exclude=atf.bat,MomoUI.fla,MomoUI.png,MomoUI.swf,MomoUI.xml,UI.atf,UI.xml