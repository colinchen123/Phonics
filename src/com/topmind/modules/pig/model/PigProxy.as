package com.topmind.modules.pig.model
{
import org.robotlegs.mvcs.Actor;

public class PigProxy extends Actor
{
    //==========================================================================
    //  Constructor
    //==========================================================================
    /**
     * 构造一个新的<code>PigProxy</code>实例.
     *
     */
    public function PigProxy()
    {
        super();
    }
}
}